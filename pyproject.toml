[project]
name = "tmrailwaysapi"
version = "0.0.1"
description = "Unofficial Turkmenistan Railways API written in Python"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.32.4",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
